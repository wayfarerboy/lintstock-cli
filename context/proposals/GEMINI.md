This directory contains the processed Markdown versions of the proposal documents.

The files here are generated from the PDFs in the `data/proposals` directory by the `npm run build:proposals` command.
